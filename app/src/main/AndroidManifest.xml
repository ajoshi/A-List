<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="biz.ajoshi.t1401654727.checkin" >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="biz.ajoshi.t1401654727.checkin.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="biz.ajoshi.t1401654727.checkin.provider.EventProvider"
            android:authorities="biz.ajoshi.t1401654727.eventprovider"
            android:enabled="true"
            android:exported="false" >
        </provider>

        <service
            android:name=".services.SWCheckinService"
            android:exported="false" >
        </service>
    </application>

</manifest>
